{
  "name": "jhpdigital-com",
  "version": "1.0.0",
  "description": "",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "develop": "concurrently \"npm run watch --prefix server\" \"npm start --prefix client\"",
    "install": "cd server && npm i && cd ../client && npm i",
    "build": "cd client && npm run build"
  },
  "engines": {
    "node": "22.11.0",
    "npm": "10.9.0"
  },
  "dependencies": {
    "concurrently": "^9.1.0"
  }
}
